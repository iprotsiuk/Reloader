# Normalize text files
* text=auto eol=lf

# C# and config/text assets
*.cs text diff=csharp
*.asmdef text
*.json text
*.md text
*.yml text
*.yaml text
*.uxml text
*.uss text

# Unity serialized assets (YAML) - use Unity SmartMerge when configured
*.unity text merge=unityyamlmerge
*.prefab text merge=unityyamlmerge
*.asset text merge=unityyamlmerge
*.mat text merge=unityyamlmerge
*.anim text merge=unityyamlmerge
*.controller text merge=unityyamlmerge
*.overrideController text merge=unityyamlmerge
*.playable text merge=unityyamlmerge

# MainWorld scene is stored in Unity binary format; force binary handling
Reloader/Assets/Scenes/MainWorld.unity -text -merge

# Project gameplay scenes can be binary in this repo; never normalize line endings
Reloader/Assets/Scenes/*.unity -text -merge
Reloader/Assets/STYLE*/Scene/*.unity -text -merge

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.tga binary
*.psd binary
*.fbx binary
*.wav binary
*.mp3 binary
*.ogg binary
*.mp4 binary
